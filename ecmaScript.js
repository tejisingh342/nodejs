/*function getBook(title, author)
{
	return {title, author};			//object short notation in ES6
}

let book = getBook("Harry Potter", "jk");

console.log(book);*/


/*const list = {
	name: "Shopping List",
	items: ["Milk", "Cow"]
};

const {items, name} = list;			//so now you can use property of object as an contant
																// no matter position of constant if name is same.
console.log(name);

console.log(items);*/


/*// arrow function
const sayLocation = location => {							//there is no need of round bracket for single parameter
	console.log(`My location is ${location}`);
};

sayLocation("Ropar");*/

